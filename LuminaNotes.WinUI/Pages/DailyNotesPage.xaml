<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LuminaNotes.WinUI.Pages.DailyNotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="8">
            <TextBlock Text="{x:Bind ViewModel.TodayTitle, Mode=OneWay}" 
                       Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="{x:Bind ViewModel.WordCount, Mode=OneWay}" 
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Editor -->
        <Grid Grid.Row="1">
            <TextBox Text="{x:Bind ViewModel.NoteContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsSpellCheckEnabled="True"
                     PlaceholderText="Start writing your daily note..."
                     BorderThickness="0"
                     Background="Transparent"/>
        </Grid>
    </Grid>
</Page>
